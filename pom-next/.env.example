# NextAuth.js secret for JWT encryption (required)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-here

# Optional: Override the base URL (auto-detected on Vercel)
# AUTH_URL=http://localhost:3000

# Set to true when behind a reverse proxy (auto-detected on Vercel)
# AUTH_TRUST_HOST=true
